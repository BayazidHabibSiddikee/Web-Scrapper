"""
frameworks/
===========
Full scraping framework examples:

1. httpx + parsel  — fast async HTTP + CSS/XPath selectors
2. Scrapy spider    — production-grade framework with pipelines
3. httpx + camoufox — async HTTP + stealth browser fallback

Install:
    pip install httpx parsel scrapy
"""

# Note: These are module stubs with full implementations.
# Run individual files for their respective examples.
