# Web Scraper Toolkit — Core dependencies
# Activate venv first: source venv/bin/activate
# NOTE: pins reflect the versions actually installed & smoke-tested in venv/.

# ── HTTP ──────────────────────────────────────────────────────────────────
httpx[http2]==0.28.1          # async HTTP client with HTTP/2
requests==2.34.2              # sync HTTP (crawlers, content extractors)
curl-cffi                      # TLS-impersonating HTTP (Scrapling's fast backend)

# ── Browser automation ─────────────────────────────────────────────────────
playwright==1.62.0            # Chromium/Firefox/WebKit automation
selenium==4.49.0              # WebDriver bindings
undetected-chromedriver==3.5.5  # Chrome anti-detect driver
camoufox==0.5.6               # anti-detect headless Firefox (TLS/canvas/WebGL stealth)

# ── Scraping frameworks (merged) ───────────────────────────────────────────
scrapling[fetchers]==0.4.15   # undetectable fetchers + spiders (StealthyFetcher/DynamicFetcher/Fetcher)
markitdown>=0.1.5             # PDF/DOCX/XLSX/PPTX/HTML/images → Markdown (universal converter)
mcp>=1.0                      # Model Context Protocol server (mcp_server.py — expose tools to AI agents)
feedparser>=6.0               # RSS/Atom ingestion (agent_tools rss_read)
browser-cookie3>=0.19         # least-privilege cookie extraction for authenticated scraping

# ── HTML parsing ───────────────────────────────────────────────────────────
beautifulsoup4==4.15.0        # DOM parsing
lxml==6.1.3                   # fast XML/HTML parser backend
parsel==1.11.0                # Scrapy-style CSS/XPath selectors
selectolax==0.3.30            # ultra-fast HTML parser (optional)

# ── Content extraction ─────────────────────────────────────────────────────
trafilatura==2.2.0            # best-in-class article extraction
readability-lxml>=0.8.1,<0.10       # Mozilla readability port
newspaper3k==0.2.8            # article + NLP (keywords, summary)

# ── Screenshots / PDF ──────────────────────────────────────────────────────
pillow>=12.3.0           # image processing
# pdfkit==1.0.0          # legacy optional HTML→PDF adapter; use WeasyPrint instead
weasyprint>=70.0        # HTML→PDF (pure Python, no binary)

# ── Screenshot CLIs ────────────────────────────────────────────────────────
# shot-scraper      → pip install shot-scraper
# chromium-screenshot → pip install chromium-screenshot

# ── Data / output ──────────────────────────────────────────────────────────
# sqlite3           → built into Python
# csv/json/markdown → built into Python

# ── Scrapy framework ───────────────────────────────────────────────────────
# scrapy==2.12.0    # full scraping framework (uncomment if needed)

# ── CAPTCHA solving ────────────────────────────────────────────────────────
# 2captcha / anti-captcha are API services, no pip package needed
# Install their Python clients if desired:
# pip install 2captcha-python anti-captcha-solver

# ── Traffic capture ────────────────────────────────────────────────────────
# mitmproxy        → pip install mitmproxy  (for traffic_sniffer mitmproxy backend)
# playwright       → already listed above (CDP HAR capture backend)

# ── Proxy rotation ─────────────────────────────────────────────────────────
# fake-useragent==1.5.1  # random UA rotation (optional)
